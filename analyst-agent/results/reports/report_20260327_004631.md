# Data Analysis Report

## 1. Dataset Overview
The dataset initially comprised 1150 rows and 20 columns. All columns are numeric, with data types including `int64` and `float64`. Specifically, columns 'A' through 'H', 'S', and 'T' are `int64`, while 'I' through 'R' are `float64`. There are no missin